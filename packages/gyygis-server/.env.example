# 本地直接运行 gyygis-server（pnpm dev）时：复制本文件为同目录下的 .env 并填写。
#
# Docker Compose 部署时，请以 **deploy/.env.example** 为完整模板，在 deploy 目录复制为 .env；
# api 容器不会读取本文件，环境变量来自 deploy/.env（见 deploy/docker-compose.yml 中 api 的 env_file）。

# 天地图（必填）
TIANDITU_KEY=your_tianditu_key_here

# 第三方地图瓦片：设为 true 时仅允许使用用户自备 key（忽略管理员代填），适合正式上线
# WEB_MAP_TILES_REQUIRE_USER_KEY=true

# 其余变量（天地图模式、FRONTEND_DOMAIN、PORT、阿里云 OSS STS、PostGIS/GeoServer 等）
# 与线上一致时，请从 deploy/.env.example 复制对应段落到本文件 .env。
