# 用户端本地端口（避免与 gyygis-admin 8848、gyygis-core 5175 冲突）
VITE_PORT=5176

# 后端 API 基址（仅开发代理用；也可在 shell 里设置 GYYGIS_SERVER_URL）
GYYGIS_SERVER_URL=http://localhost:3000

# 底图在线补洞：off | auto | on（可与 server MAP_ONLINE_POLICY 一致；启动后会拉 /api/map-basemap/config 覆盖）
VITE_MAP_ONLINE_POLICY=auto
