import type { DockviewTheme } from "dockview-core";

/** 大屏数据可视化：深蓝底 + 青蓝高光（自定义 className，样式见 styles/dockview-theme-gyygis-bigscreen-blue.css） */
export const themeGyygisBigScreenBlue: DockviewTheme = {
  name: "gyygisBigScreenBlue",
  className: "dockview-theme-gyygis-bigscreen-blue"
};
